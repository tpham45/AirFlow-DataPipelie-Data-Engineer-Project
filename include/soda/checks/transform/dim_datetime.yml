checks for dim_datetime:
  - schema:
      fail:
        when required column missing:
          - datetimeindex_id
          - pick_datetime
          - pick_day
          - pick_month
          - pick_year
          - pick_weekday
          - drop_datetime
          - drop_hour
          - drop_day
          - drop_month
          - drop_year
          - drop_weekday
        when wrong column type:
          datetimeindex_id: INT64
          pick_datetime: TIMESTAMP
          pick_day: INT64
          pick_month: INT64
          pick_year: INT64
          pick_weekday: INT64
          drop_datetime: TIMESTAMP
          drop_hour: INT64
          drop_day: INT64
          drop_month: INT64
          drop_year: INT64
          drop_weekday: INT64
